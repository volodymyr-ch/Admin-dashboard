export enum RoutePath {
  home = '/',
  app = '/app',
  main = '/app/main',
  analytics = '/app/main/analytics',
  dashboard = '/dashboard',
  users = '/users',
  email = '/app/inbox',
  grid = '/app/grid',
  notFound = '*',
}
